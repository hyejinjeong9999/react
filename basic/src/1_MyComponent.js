import React from "react";

/**
 * 1. 컴포넌트 생성
 *
 */

const MyComponent = () => {
    return <div> 내 첫번째 컴포넌트</div>
}

/**
 * 다른 파일에서 이 파일을 import할 때
 * 위에서 선언한 MyComponent 클래스를 불러오도록 설정
 */
export default MyComponent;

